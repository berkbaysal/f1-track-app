import { CssVarsProvider, extendTheme } from '@mui/joy/styles';
import { fontFamily } from '@mui/system';

export const formulaTheme = extendTheme({
    fontFamily:{
        body: "formula1-regular"
    }
})